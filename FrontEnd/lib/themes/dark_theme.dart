import 'package:flutter/material.dart';

class DarkTheme {
  /// Primary Background: Main background color for views.
  static const Color primaryBackground = Color(0xFF1C1C1E);

  /// Secondary Background: Secondary-level background color (e.g., grouped lists, secondary panels).
  static const Color secondaryBackground = Color(0xFF2C2C2E);

  /// Tertiary Background: Tertiary-level background color (e.g., cards or grouped content).
  static const Color tertiaryBackground = Color(0xFF3A3A3C);

  /// Primary Text (Label): Primary text color on dark backgrounds.
  static const Color primaryText = Color(0xFFFFFFFF);

  /// Secondary Text: Secondary text color (e.g., sublabels, secondary info).
  static const Color secondaryText = Color(0xFFEBEBF5);

  /// Placeholder/Hint: For placeholder text or hints.
  static const Color placeholder = Color(0xFF8E8E93);

  /// Separator: For dividers or borders between elements.
  static const Color separator = Color(0xFF48484A);

  /// Accent (Link): Default blue accent color (e.g., links, tappable text).
  static const Color accent = Color(0xFF0A84FF);
}
