import 'package:flutter/material.dart';

class LightTheme {
  /// Background color seen in the screenshot
  static const Color backgroundLightBlue = Color(0xFFD8EAF2);

  /// A darker blue used for primary actions (e.g., buttons)
  static const Color darkBlue = Color(0xFF002E8B);

  static const Color blue = Color(0xFF1388BE);
  /// Standard dark text color
  static const Color textDark = Color.fromARGB(255, 0, 0, 0);

  /// White text for use on dark backgrounds/buttons
  static const Color white = Color(0xFFFFFFFF);

}
